using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace CoffeeMachine
{

    public class CoffeeMachine : MonoBehaviour
    {
        [SerializeField] private Container waterTank;
        [SerializeField] private Container coffeeBeansContainer;
        [SerializeField] private Container dripTray;
        [SerializeField] private Container groundContainer;

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}